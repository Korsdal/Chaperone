# Session Log — August 2026

> Child document of `LOGBOOK.md` (logbook protocol v1.0). **Most recent first.**
> Pilot install on customer hardware, data-integrity fixes, admin authority, open-sourcing.
>
> The **newest** session entry always lives in `LOGBOOK.md` itself, so a cold
> `/logbook start` can read `Next session start from:` without opening this file.
> At each `/logbook end`, the outgoing newest entry is moved down into its month
> file here before the new one is written.

---

### Session 2026-08-28 — jok / Claude
**Type:** review (codebase validation) → direction decision
**Focus:** validate a conference-talk review against the code, then use what it found to re-ask where the product is heading. The validation was the assignment; the direction decision is the session's real output, and it moved the roadmap's centre of gravity.

**Worked on:**
- [x] **Validated all 25 verifiable claims** in `specs/chaperone-talk-validation.md` — an external reader's 27 questions, answered by jok *from memory* and then critiqued, carrying 35 `VERIFY:` directives. Two workflows (27 agents), every verdict re-derived by one or two independent agents prompted to *refute* it. Findings in `specs/chaperone-talk-validation-findings.md` (local). **7 of the 27 answers are contradicted by the code** — including the two most quotable, and both in the *safe* direction: the lease is taken at **write** time, not read time (a read cannot hold one — `read()` is not even passed a `LeaseManager`), and writes **fail closed** on an unreachable coord, not open.
- [x] **The adversarial pass earned its cost.** A verifier falsified a universal its own tracing agent had asserted — "every `CoordUnreachable` arises before bytes are written" is true of `chapr_write` and false of `chapr_move` and `restore(copy)`. That is the finding nobody asked for, and the best thing in the report.
- [x] **Three probes, run and reverted** (`git status` clean afterwards, 381 tests green, clippy clean). Two concurrent writers with coord stopped: **both refused, nothing written, no sidecar** — which answers the reader's "tested how?", because no coord-unreachable *write* test of any arity existed. Two concurrent creates: exactly one winner, the loser gets "already exists" — creation is **guarded**, not the unguarded path the document feared. And BLAKE3 measured at **~4 GiB/s** (a 50 MB tender ≈ 12 ms), so the reviewer's hash-latency worry is misplaced: the cost is the two SMB reads and the 256 MiB pre-image upload, not the CPU.
- [x] **Corrected two of my own findings before they became work.** **D-1** (`chapr_move` mutates before it talks to coord) is **D-013's recorded decision** — *"SMB rename FIRST … the gap between them is the unavoidable no-cross-system-transaction window (explicit non-goal); a failure there leaves coord stale-but-recoverable."* The ordering is settled and right. What is actually wrong: the agent is told *"NOTHING WAS CHANGED"* after a rename that succeeded (the I-015 pattern again — sound mechanism, harmful message), and **"recoverable" was asserted but never built** — no reconciliation exists anywhere in coord, and the move takes no journal entry. **D-2** (overwrite-move discards src history) is likewise D-013's, with three unexamined consequences: it is unaudited, the blobs become GC-eligible because reachability is computed from `version_log`, and §12 calls that metadata *"append-only, immutable."*
- [x] **And withdrew a recommendation.** The findings suggested a normalised `.docx` hash inside Chaperone as a conflict *classifier*. Wrong: unzipping OOXML is interpreting content, D-039 is a **product** boundary that *"would hold even if that were free"*, and D-006 means coord could not recompute such a hash without a parser. Its home is **E-028**. The probe itself stands — two semantically identical `.docx` files, different bytes, CAS refuses, and the normalised hashes match exactly.
- [x] **Three genuinely unbooked items**, with no issue or backlog ID between them: **audit-log integrity** (no chaining, no append-only enforcement, no verifier — and `POST /audit` takes `kind`, `path`, `session_id` and `detail` verbatim from any authenticated caller while *reading* the trail needs `AdminAuth`); **storage limitation** (§12 specifies 1–3 years, `audit_log` and `version_log` are never pruned at all, and no privacy documentation exists anywhere in the repo); and **session identity is a process ID** (`sess-{pid}`, `main.rs:76`), which is *why* "which agent did this, who prompted it" cannot be answered.
- [x] **Re-asked the direction rather than re-sequencing the roadmap**, on jok's instruction: *"it's not the most important thing that we follow the roadmap already in place with these new issues, but that we decide again — where are we heading with this product?"* New roadmap at `specs/chaperone-roadmap-2808.md` (local), superseding `chaperone-roadmap-0.2-to-0.5.md` and preserving its item IDs.

**Direction settled (jok)** — stated in full in Current State above, so not repeated here. The one part that was jok's correction of *my* framing, and which changed the ranking: Apache-2.0 means nobody signs an invoice, so the audit trail is not a sales artefact but what makes it reasonable to let agents write to a shared drive at all. That is why accountability ranks second rather than last, and it retired the GRC framing I had brought.

**The consequence worth carrying.** The previous roadmap's largest remaining investment — **Track B, the document gap** (Branch A ~3–4 sessions, Branch B ~8–10) — is **off the path**, and **D-D′, the project's only open architectural gate, resolves to "the line holds."** Both should be closed *with reasoning*, not left looking planned; the old roadmap's silent deletion of item 3.5 is the failure mode to avoid. 2.3 got cheaper (the logbook is already public and scrubbed). **I-007 only partly did, and I corrected this while writing the entry:** the CI e2e job means the SMB rig is *written*, not *working* — the 08-21 record is explicit that after two runs everything Windows-specific is still unproven, including the mandatory-lock check, *"the only automated evidence for invariant 3 this project would have."* So getting that leg green became **B0**, a prerequisite of the whole correctness phase rather than a convenience inside it.

**State changes:** **no code changed and no version change** — `0.1.3` stands, 381 tests, clippy clean, `git status` clean. Two new local documents under `specs/`. Nothing was written to the decision log, ISSUES or BACKLOG (see below). Current State rewritten.

**Open questions:** 21 in the new roadmap's §6. The four that decide the most: **Q6 — where does a per-conversation session id come from?** MCP does not obviously supply one, and if the answer is "it cannot", Phase C delivers tamper-evidence over a record that still cannot attribute — worth knowing *before* building the migration machinery. **Q4 — chain immutability versus erasure**: a chain wants immutable rows, storage limitation wants them gone; the proposal is a tombstone whose hash still links. **Q1 — does 5.1 chunked reads survive on its own merits?** It is a read-path capability rather than extraction, and the old roadmap rates it highest for user-visible value. **Q12 — is a GitHub-runner SMB share enough to unblock I-007**, or does it wait for the customer's Windows Server 2022?

**Deliberately not written this session:** the four decision entries the session earned — the direction itself (`product`, beside D-037 and D-039), **migrations** (the only one where no record exists in either direction; D-003 claims persistence *"establishes the migration/pool layer"*, which was never built), the audit-integrity posture as an amendment *scoping* D-024's *"not court-grade non-repudiation"* rather than reversing it, and Track B's disposition. Left for jok because the direction's wording is his, and because §6's Q21 asks about them. Also unwritten: nine drafted ISSUES entries sitting in the findings file, and Q20's housekeeping — the duplicate `D-038` row, `D-009` never issued, missing `Status` trailers on D-001 and D-032, and six live issues breaching the 30-day STALE rule.

**Next session start from:** **`specs/chaperone-roadmap-2808.md` §6** — answer **Q1/Q2** (Track B and D-D′ disposition, because closing them retires the project's only open architectural gate) and **Q6** (session identity, because Phase C's value depends on it). Then **Phase A, "Truth"**, which needs no answer to anything: the `chapr_move` message that claims nothing changed when it did (`server.rs:897-901`; reuse `CommittedButUnrecorded`'s pattern from `write.rs:156-160`), the four false comments about `restore(in_place)` (`ops.rs:287-288` the worst), the last Q17 residual at `server.rs:1133-1137`, the stale 512 KiB at `server.rs:54/56/2278` — already booked as the old roadmap's §9.11, and it survived Phase 1's doc pass — `CLAUDE.md`'s unimplemented "SSPI/Kerberos" claim, the concept spec still asserting the two channels "never cross", and promoting this session's three probes to permanent tests. **Carried forward, still unanswered:** does the extraction pipeline write explicit UTF-8? Somebody still has to ask Kristian, and it still sets I-015's real severity.

---

### Session 2026-08-25 — jok / Claude
**Type:** engineering (bug hunt → correction → boundary decision)
**Focus:** a suspected regression in the read path, brought as a written brief with an explicit instruction to verify before fixing. It confirmed, but the fix that followed was not the one the brief was costing — jok's reframing turned it from a capability question into a product boundary, and that is the session's real output.

**Worked on:**
- [x] **Verified the brief's claim, and refuted half of it.** `binary_guard`'s second clause treated "not valid UTF-8" as "not text", so every CP1252 or UTF-16 file was refused as "an unrecognised binary format … worth their attention". Reproduced against the real function with five fixtures; prior behaviour at `abefc61` confirmed by reading the tree (`render_envelope` byte-identical, no guard, so base64 with `encoding=base64`). **But the regression is narrower than it looked:** base64 Danish prose was never analysable either, so what was lost is *default round-trippability* and *accurate diagnosis*, not readability. The refusal **policy** was always defensible. Findings in `specs/bug-hunt-2508-findings.md` (local).
- [x] **jok's reframing, which changed the work:** *"It is not a file extraction service, it is a coordination service — and the coordination part works. The extraction of useful text is not Chaperone's job… it should not give the message that something is wrong with the service itself. The issue is the files."* Recorded as **D-039**, which also chose I-005's long-standing third option (declare it out of scope and coordinate the derived artifacts) and generalised it beyond PDFs. Capability booked as **E-028**, a separate deployable.
- [x] **Fixed message and diagnosis, deliberately not capability.** `sniff::classify_unrecognised` (BOMs, NUL parity for endianness, high-byte ratio — `std` only, no dependency) splits unrecognised bytes into text-in-another-encoding versus binary, and **chooses a message, never an outcome**: both arms still refuse, which is what keeps its thresholds harmless. `RefusalKind` replaces `container: Option<_>`; the text class gets its own frame that never says "binary", separates Chaperone's health from the file's state in the first sentence, states the boundary in-band, and gives the human a remedy. All five classes gained the boundary sentence.
- [x] **The diagnosis now reaches the people who can act on it.** A `NON_UTF8_TEXT` warning carries the structural evidence through `diag.rs` — which was built for exactly this and which the refusal path had **never reached**, since it returns before `tool_failure`. Coord groups by `(code, path)` and the remedy points at the *producing step*, so a folder of legacy files reads as one fix. Container refusals file nothing: a PDF on a share is a designed outcome, the same line `classify` already draws. Facts are structural only — no content excerpt, so §13.2's existence leak is not widened.
- [x] **jok's follow-up question found the hole the fix had left**, and it was the sharpest moment of the session: *if extraction is agentic and writes through Chaperone, does Chaperone create files it then refuses to read?* Answer, measured: **no through `utf8`** (`String::into_bytes()` is valid UTF-8 by construction — an agent authoring text has no encoding to get wrong), **yes through `base64`** (nothing guards the write path). The reachable sequence is partly our own making: a read is refused → the refusal names `allow_binary` for copying → an agent told "produce a mirror" reads "copy" as its job → the same unreadable encoding lands somewhere new.
- [x] **Mitigated with guidance, on jok's call** — the rule now sits in `server::instructions()` beside D-028's write-routing rule, naming the specific loop, plus one clause on `ContentEncoding::Base64`'s schema doc. jok's reasoning: *"If the model understands it, they can avoid it, and better, they can explain to the user what went wrong."* A symmetric write guard was **rejected**: it would refuse byte-exact copying, which is `allow_binary`'s one legitimate use. D-039 amended to record all of this against itself.
- [x] **Two invariants pinned that were load-bearing and unasserted.** `any_utf8_write_can_be_read_back_as_text` (five hostile cases) and the companion test recording the base64 hole rather than hiding it. The first was **mutation-checked** — `decode_content`'s `utf8` arm temporarily made to emit `0xE6`, test went red naming offset 36, reverted and diffed byte-identical. A green test that cannot fail pins nothing.

**Verified:** `cargo test --workspace` **381 passing** (was 364), 0 failed; `cargo clippy --workspace --all-targets -- -D warnings` clean. Serve-versus-refuse is byte-for-byte unchanged — the three pre-existing round-trip tests pass untouched, and `classifying_the_bytes_never_serves_them` pins that no read that worked before behaves differently. Every throwaway probe reverted and `git status` confirmed clean between passes.

**State changes:** version `0.1.2` → **`0.1.3`** (jok, this session); tests 364 → 381; `sniff.rs` gains the classifier, `server.rs` gains `RefusalKind` + `report_encoding_finding` + the encoding paragraph in `instructions()`, `diag.rs` gains `record()` for findings that are not `ChaprError`s. New: **D-039**, **I-015**, **E-028**. `docs/architecture.md` updated for both refusal classes.

**Found and deliberately not fixed:** a UTF-8 file *with* a BOM is served as text with `U+FEFF` inside the envelope body, and nothing handles BOMs in either direction (I-015 residual 2).

**Open questions:** (1) **Does the extraction pipeline write explicit UTF-8?** — for Kristian, and it is the one that sets I-015's real severity: Python's `open(p,'w')` with no `encoding=` resolves to cp1252 on a Danish-locale box, so if any text mirror is written that way then every mirror containing a Danish character is refused, which is the pilot's main workload failing on its main content. No amount of code settles this. (2) **Does the write-time diagnostic get built?** — option (c) from this session, held in reserve: a write that lands bytes a later read would refuse could file its own `NON_UTF8_TEXT` at creation, attributed with version and principal, closing the loop provably rather than advisorily. Deferred on the judgement that guidance is the right strength first; the trigger to revisit is the pilot showing an agent walking into the loop anyway. (3) **The decision index carries two identical `D-038` rows** — pre-existing, spotted while adding D-039, and left alone rather than silently edited; deduping it is a one-line call for jok.

**Next session start from:** **a code review of the solution as it now stands** — phase 1's edits plus this session's safeguard, reviewed as one body of work rather than as two changes. Worth pointing it at the read path specifically: `sniff.rs`'s thresholds have never been exercised against anything but chosen fixtures, `binary_guard`'s new shape is one session old, and the `NON_UTF8_TEXT` diagnostic's admin-page rendering is still **unexercised** — steps 4 and 5 of that plan's verification need the real deployment and could not be run here. **Then phase 2 of the 0.2→0.5 roadmap** (`specs/chaperone-roadmap-0.2-to-0.5.md`, local). Carry into it: **the question for Kristian** — does the extraction pipeline write explicit UTF-8? Python's `open(p,'w')` with no `encoding=` resolves to cp1252 on a Danish-locale box, and if any mirror is written that way then every mirror with a Danish character in it is refused, which is the pilot's main workload failing on its main content. That answer sets I-015's real severity and nothing in code can settle it.

---

### Session 2026-08-21b — jok / Claude
**Type:** engineering (roadmap review → phase 1 delivered)
**Focus:** review the untracked 0.2→0.5 roadmap against the code, then run phase 1 through. Ended with a scope decision (D-037), five delivered items, a version bump, and the logbook itself going public.

**Worked on:** *(per-item detail lives in Current State above and in the roadmap; this is the record of what happened, not a second copy of what it does)*
- [x] **Audited the roadmap proposal item-by-item against the tree** — three parallel explore passes (coord auth/TLS/GC, endpoint read path/tools/watcher, CI/examples/leases/docs). Verdicts + `file:line` evidence in `specs/reviewed_roadmap.md` (local); plan rewritten as v2 with v1 preserved beside it. **Item IDs kept stable** so the audit's verdicts still map 1:1. The audit's own findings mattered as much as the plan: 1.2 was already ~90% shipped, 4.2's lease mechanism does not work as the plan assumed, and 1.1's acceptance criterion already passed before anything was built.
- [x] **D-037** — on-prem is the product; cloud deferred on a *market judgment*, not an architectural exclusion (review 2027-02-21). New `product` decision theme. Phase 6 rescoped to linkage-only; identity stays pluggable.
- [x] **Phase 1 delivered, all five items** — 1.3 binary read guardrail (new `sniff` module), 1.2 TLS residue, 1.1 authenticated control channel (new `endpoint_token` module + `Authenticated` extractor over nine unguarded routes), 1.5 macOS in CI, 1.4 the e2e job. **Tool count unchanged at 11** — phase 1 added no tools, which item 3.2's reasoning about context cost required.
- [x] **Version → 0.1.2, set by jok.** A patch bump, deliberately: "it stays 0.1.x until it is tested and true."
- [x] **The logbook is now tracked (D-038)**, reversing D-033's exclusion of it, with customer identifiers scrubbed first. Also **`specs/`**: a new gitignored directory for jok's working material, so `docs/` holds only what a reader needs (three files, down from nine).
**Three bugs no test could see, all found by running the thing rather than testing it:**
- **`serve` never validated its config.** `Config::load` went straight to `run_server`; the structural rules were enforced only by the wizard's `probe` and the admin settings API. So a **hand-written** config — exactly what the shipped template invites — started a server with the auth allow-list, bind-address-as-URL and loopback rules all unchecked. Found by pointing `serve` at an https URL with no `[tls]` and watching it come up happily. `validate()` now runs inside `load`, after `apply_overrides` so an env var cannot smuggle past it.
- **The non-interactive wizard produced TLS with an `http://` URL.** The only scheme reconciliation lived inside `interactive_fill`, which `--non-interactive` skips entirely. Merely wrong before; **fatal** once the mismatch rule landed, and it would have broken the scripted installer.
- **The self-test reported "coordinator reachable" with no credential.** `/healthz` is deliberately open, so a wrong token surfaced several checks later as something unrelated. It now probes an authenticated route and stops with the real diagnosis.

**Verified, not assumed:**
- Every CI step run **verbatim locally on Windows** before being written into the workflow: config generation, hostname startup, the `pkill`→`taskkill` fallback, all four enforcement assertions, and the self-test at **8 passed / 0 failed / exit 0** — including *"a second exclusive open was refused while the first was held"*.
- Enforcement checked over real HTTP with curl, not only in unit tests: issued token → 200 with a real body; forged 64-char token → 401; principal header alone → 401; `journal/clear` unauthenticated → 401; `/healthz` → 200.
- Both smoke suites green against a live coordinator (`smoke_parts` 14/14, `smoke_pilot` 11/11 in 7.4 s including a 16 MiB blob).

**Mistakes of my own, recorded because they will recur:**
- **`sed` on Rust source bit three times.** An insertion anchored on a fn name landed *under* the `#[tokio::test]` belonging to the next function; a `\n` inside a temporary `println!` became real newlines, so deleting "the line" left five orphaned fragments; and a backslash in a folder-name pattern failed the whole expression. Use the file tools for code.
- **The worst one was nearly silent.** After removing an orphaned attribute the test count came out 182 where 183 was expected — chasing that single-test gap found I had **disabled an existing auth test** (`content_routes_require_an_identity_under_enforced_auth`): still compiling, no longer running. Count your tests.
- **Overreached twice on E-015**, claiming on-prem-only collapses it to `negotiate` and kills OIDC's rationale. Wrong both times: D-023 chose OIDC for three reasons and only the cloud one weakens here. Recorded in D-037 rather than quietly corrected.

**After the first CI runs — three commits, and one lesson worth more than the fixes:**
- **`edbc8eb`** — run 1 died on windows-latest at its *first step*, `cargo build`: `Could not resolve host: static.crates.io`. Cause was CI's new shape, not the runner alone — three jobs with warm caches became six, four brand new, all doing a cold `cargo fetch` in parallel. Fixed with `needs: check` (e2e reuses the cache `check` just saved, and skips entirely on a red build), a per-OS `shared-key`, `CARGO_NET_RETRY: 10`, sparse registry. It did not recur, so: transient, load-related.
- **`d4db4de`** — two real faults. clippy `useless_format` (`config.rs:554`), **not** a phase-1 regression: the line dates from `b1e7814` and only failed once CI's stable clippy reached **1.98.0**. Deleted rather than given clippy's `.to_string()` fix, which would have preserved a tautology. And macOS e2e died on **`sed -i`** — a GNU-ism; BSD sed reads the script as a backup suffix. Fixed by not editing the file: `CHAPR_COORD_AUTH=shared-secret` uses the documented defaults→file→env precedence.
- **The lesson, worth more than either fix: "clippy clean" was measured on the wrong toolchain.** Local was **0.1.97**; CI enforces `stable` = **1.98.0**. Installing 1.98.0 locally found a **second** lint the reported legs structurally could not show — `chunks_exact_to_as_chunks` in `watch_win.rs`, `cfg(windows)`, so never compiled by ubuntu or macOS. Windows hit it the moment its build cleared DNS. Fixing only what was reported would have bought another red. `as_chunks::<2>()` confirmed on MSRV by compiling against 1.88.0, not by reading release notes. Now green at both ends: clippy + 364 tests on 1.98.0, build + tests on 1.88.0.

**State changes:** version `0.1.2`; 364 tests; new modules `chapr-endpoint/src/sniff.rs` and `chapr-coord/src/endpoint_token.rs`; new `e2e` CI job (3 legs); new `product` decision theme; `LOGBOOK.md` + `logbook/` tracked. Docs corrected where they described the old behaviour: `security.md` (the control plane authenticates now, with the two remaining limits stated plainly), `architecture.md` (read limits + auth), `deployment-guide.md` (TLS and auth as explicit wizard steps), README, the MCPB manifest, and the coord config template.

**Open questions:** (1) **`cargo fmt` is still not in CI** — 365 files drift from rustfmt, so adding it means reformatting the tree. Mechanical, but a separate commit and jok's call. (2) **1.4 covers 2 of its 5 named scenarios**: the lease-leak suite needs a fault-injecting proxy that does not exist in the repo, and kill-mid-write/journal-recovery and the Office `~$F` refusal have no example at all. (3) **The e2e job has now run twice, and what it proved is narrower than green.** Actions accepts the YAML, and the macOS leg reached the auth-restart step — so share setup, coordinator startup and **both smoke suites passed on macOS**, the first end-to-end validation there (I-012). Still unproven after two runs: **everything Windows-specific** — `New-SmbShare`, the coordinator over UNC, and the mandatory-lock check, which is the whole point of that leg — plus the auth-restart step and the self-test on any leg. (4) **D-D′ is the gate on Track B** and needs jok + Kristian, not code.

**Next session start from:** **the CI run at `d4db4de`** (pushed; two earlier runs are already analysed above). Both 1.98 lints are fixed and verified against CI's exact toolchain, so `check` should be green on all three OSes; `e2e` then runs for the first time with the `sed` fix in place. **The one thing still entirely unproven is the Windows leg past clippy** — `New-SmbShare`, the coordinator over UNC, and the mandatory-lock check, which is the only automated evidence for invariant 3 this project would have. If it breaks there, that is real and not a one-line fix.

Then, in order: **jok's two carried-over items** — the ssh-agent decision (recommendation: leave the passphrase human-gated, and treat "jok pushes" as policy rather than a defect) and nothing else outstanding on the blocker now that its diagnosis is corrected above. Then **phase 1's leftovers** in open question (2) — the fault proxy, the kill-mid-write and Office-lock examples, and the `cargo fmt` reformat call. Then the **D-D′ conversation** before anything in Track B gets designed.


### Session 2026-08-21 — jok / Claude
**Type:** engineering (project memory)
**Focus:** "It is time to split the logbook, it has grown well beyond the recommended size." Splitting it turned out to be the small half — the load-bearing part was that every child doc inherits D-033's disclosure problem, and that the section nobody had set a threshold on was the one costing the most.

**Worked on:**
- [x] **Measured before proposing.** Every threshold-governed section was over: Decision Log 96,442/8,000 (12×), Session Log 81,564/10,000 (8×), Backlog 18,357/5,000, Known Issues 12,848/5,000. Total 233,764. Found the Backlog's own inline note from **2026-08-05** recording the breach and that archiving "needs a human call" — so this was a five-week-old standing question, not a new observation.
- [x] **Split executed and recorded as D-036.** Four axes, each jok's call: decisions **by theme** not by year · child docs under **one `logbook/` directory** · root file becomes an **index** · **newest session entry stays in root**. Eight child docs. `LOGBOOK.md` **233,764 → 27,282 chars (88% smaller)**, every threshold-governed section now under its limit.
- [x] **The gitignore hazard, which was the real find.** The protocol's default paths (`decisions/`, `logs/`, `ISSUES.md`, `BACKLOG.md`) are covered by **no** existing rule, so a split at those paths would have staged 96 KB of candid internal reasoning into a **public** Apache-2.0 repo — the exact thing D-033 purged from history at a force-push cost recorded as unrepeatable. Rule is `logbook/` as a **directory**, deliberately not a file list, so a child doc added later is covered by construction.
- [x] **Current State rewritten, not archived (20,539 → 3,918).** It is marked VOLATILE and had been appended to for a month instead. No threshold governed it, which is why it grew unchallenged while the sections that *had* thresholds at least got flagged. Displaced narrative moved verbatim to `logbook/state-history.md`.
- [x] **Anchors added because the links I had written were broken.** The index promised `#d-035` jumps; markdown renderers generate full-title slugs, so explicit `<a id="d-nnn"></a>` anchors went into the child docs. 42 root links verified against real anchor targets.
- [x] **`CLAUDE.md` + `.gitignore` updated.** The vendored `.claude/commands/logbook.md` was deliberately **not** edited — it is shared AgentHarness tooling; the project-local theme rule lives in `LOGBOOK.md`'s own YAML instead.

- [x] **`/logbook audit` run afterwards — the project's first.** Verified every claim in Current State against the code rather than against itself. **Correct:** version `0.1.1`, MSRV `1.88.0`, three crates, Apache-2.0 + LICENSE + NOTICE, six admin tabs, and **tests exactly as claimed — 166/126/32 = 324, all passing, 0 failed**. Every child-doc count and index row matched too. **Stale:** coord routes claimed **11**, actual **29**; MCP surface claimed **10 tools**, actual **11** (`chapr_restore`); and `CLAUDE.md` still said version `0.1.0` with "the next bump is unblocked and is jok's call" — a gate that closed on 2026-08-20. All corrected in both files.
- [x] **The audit's real finding is a pattern, not three numbers.** I-004 was filed on 2026-08-03 for exactly this (a stale `CLAUDE.md` §Status, auto-loaded before the logbook can correct it), fixed 2026-08-14, and had gone stale again within a week. Kept **OPEN and widened** to the recurring issue, severity LOW → MED, with the original entry preserved verbatim beneath the rewrite. The cause — that every drifting claim is a code-derived count nobody checks, though each is a one-line command — is filed as **I-013**, with three options and a note against over-engineering it. **I-002 also corrected:** it argued from "9 of 22 coord routes" gated; it is 29, so the ungated set E-015 must audit is **20, not 13**.

**Verified, not assumed — a split is exactly where memory gets silently truncated:**
- All **34** decision bodies, **22** moved session entries and **33** backlog rows `cmp`-verified byte-identical against a pre-change backup. All **12** issue narratives byte-identical to their source table cells.
- LF endings and UTF-8 checked deliberately, given last session's `perl -0pi` double-encoding incident. Zero CR, zero mojibake.
- Cold-start test: `LOGBOOK.md` alone answers phase, status, active sessions, live issues and `Next session start from:` without opening a child doc.

**Mistakes of my own, recorded because they will recur:**
- **`gsub(/\\|/, ...)` in gawk parses as "backslash OR empty"** and matches at every position, so a pipe-protection pass silently mangled every field it touched. It produced a plausible-looking empty output rather than an error. Use a bracket expression (`/\[|]/`), or avoid the escaping question entirely — which is what I did in the end, rejoining overflow fields instead of regexing.
- **Two verification "failures" were my measurement, not the data** — `grep -c $'\r'` matched every line (empty pattern, so a false CRLF report on nine files), and a session-log comparison failed because my extraction swept up a child doc's header block. Both times the artifact looked like real corruption. Check the instrument before reporting the finding.
- Claimed "~60 more decisions fit" in a header note; the real figure is ~15. Corrected in-file before installing.

**State changes:** `LOGBOOK.md` is now an index (27,282 chars) plus eight child docs under `logbook/` (~220 KB), all gitignored as one directory. Decision Log 6,220/8,000 · Session Log 8,988/10,000 · Known Issues 1,880/5,000 · Backlog 1,169/5,000. D-036 recorded in `logbook/decisions/process.md`. `/logbook end` now has a new first step: move the outgoing newest entry into its month file (exercised for the first time this session, moving 2026-08-19/20 into `logbook/logs/2026-08.md`).

**Open questions:** (1) **Session Log headroom is bounded by one entry** — the section is the ~1.3 KB index plus a single entry, so anything over ~8.5 KB breaches the 10,000 threshold on its own. It stood at 8,988 immediately after the split and 6,151 once the outgoing entry moved out, so the figure swings by whichever entry is resident. (2) **The decision index fits ~15 more rows** before it needs its own call. (3) **`logbook/` still has no home in a private repo** — the 2026-08-14 backup argument is now spread across nine local-only files instead of one, which makes the single-laptop risk worse, not better.

**Next session start from:** **`logbook/ISSUES.md` I-013** — decide how the docs' numeric claims get checked: a CI step, a standing item in `/logbook audit`, or stop asserting counts in prose and let the binaries report them. The entry argues (c) scales and (b) is the right size for a two-person project; it needs a human call, and it is cheap. **One small thing first:** D-001 and D-032 still carry no `**Status:**` line (indexed `CURRENT ᵃ` by inspection) — left for jok because it is a claim about a decision's standing. Then the pre-existing queue, unchanged by this session: classify the pilot's drive-mapping finding, the macOS CI run (I-012), and `gh release create` (I-011).

---

### Session 2026-08-19/20 — jok / Claude
**Type:** engineering (licensing + distribution)
**Focus:** "Apache 2 this lil project." That turned out to be the small half. Making the project *actually* open — binaries people can install, a repo that does not leak internal reasoning, and a delivery format that is not one vendor's — was the session.

**Worked on:**
- [x] **Apache-2.0 (D-033).** Canonical LICENSE (`md5 3b83ef96…`, fetched not retyped), NOTICE, `license = "Apache-2.0"`, SPDX headers on 64 files, `"license"` in the MCPB manifest, README rewritten. `publish = false` kept — open source and on-crates.io are separate decisions. `.mcpb` bundles now carry LICENSE + NOTICE **inside**, because the bundle is a §4(a)/(d) redistribution landing on a laptop that never sees the repo.
- [x] **Attribution revised on jok's reasoning.** NOTICE names the two owning companies and the two contributors, and deliberately **not** the owner who authorised the relicense: the copyright holder is the entity, listing a non-contributor under "Contributors" would be false, and NOTICE files get scraped into SBOMs, so a name in one attracts mail. Consequence to hold onto — the authorisation record now exists *only* in D-033 here.
- [x] **Releases are CI-built artifacts on a tag (D-034).** `ci.yml` (build/test/clippy on both OSes + MSRV) and `release.yml` (test, build, pack, checksums, attest, **draft** for a human to publish). The tag must match the workspace version or the run fails — the bump is the decision, the tag only records it. Explicitly *not* the customer deployable's committed-binaries model: that is right for a repo cloned onto a coord host, wrong for a source repo where it puts a 9 MB blob in every clone and creates an artifact with no verifiable link to a commit.
- [x] **`LOGBOOK.md` + `CLAUDE.md` removed from the repo *and* from history.** The 2026-08-14 decision to commit them rested on "the repo is private, so there was never a disclosure argument" — a premise that expired. Two `filter-branch` passes: one for the files, one scrubbing the customer solution name, a collaborator's workflow name and an internal hostname from all 31 commits *and* their messages. Then reflog expiry + `gc --prune=now`, verified by decompressing **every** blob in the object store: zero hits. Backup bundles taken first (`~/Desktop/chaperone-pre-rewrite-2026-08-20.bundle`).
- [x] **Repo split rather than force-pushed.** Force-push alone would **not** have worked: GitHub's `refs/pull/*/head` refs survive it, are not user-deletable, and are trivially enumerable — so on a public repo the logbook would still have been two commands away. Old repo renamed to `-archive` (private, keeps its PRs), new public repo took the canonical name, so the five URLs already in the docs stayed correct.
- [x] **Vendor-neutral delivery (D-035).** `chapr-endpoint print-config <host>`, bare binaries for every platform, macOS added, and the server now identifies as `chaperone` rather than as `rmcp`. See D-035; it is the substantial decision of the session.
- [x] **MSRV measured instead of estimated.** CI caught `1.85` being fiction — the locked graph needs `1.88.0` (`darling`, `time`). Wrong twice now, both times from reasoning about a couple of direct dependencies instead of measuring across the graph, so the fix is the method: `cargo metadata`, and the CI job now **reads `rust-version` out of `Cargo.toml`** instead of pinning its own copy, which was itself the second place for the number to live.

**Found by running it rather than reading it:**
- [x] **`build-mcpb.ps1` took `$ChaperoneRoot` from `$PSScriptRoot` as a param default** — empty when a script is invoked as `powershell -File`, which is exactly how CI invokes it. It had only ever worked because callers dot-invoked it. Would have failed the first release and looked like a CI problem.
- [x] **Three Linux CI failures, revealed one at a time** because cargo stops at the first failing test binary and clippy runs after tests. Two were tests asserting a portability `std::path` does not offer (a backslash is an ordinary filename character on Unix, so `C:\ProgramData\Chaperone` is *one* component); the third was `shares.rs` dead code, since off Windows the offerability filter's only caller is its own tests. No production function changed. Lesson applied thereafter: run the **whole** suite on the other platform, not the named failing test.
- [x] **A genuine macOS safety bug.** `/var`, `/etc` and `/tmp` are symlinks into `/private`, and `harden_data_dirs` checks the *canonicalised* path — so `/var/lib` arrived as `/private/var/lib`, matched nothing, and `restrict_dir` went on to chmod a shared system directory. **Only the runner's permissions stopped it; setup runs elevated.** Precisely what the guard exists to prevent. Fixed by stripping a leading `private` before both rules, unconditionally rather than behind `cfg(target_os = "macos")` — one path exercised everywhere beats a branch only one platform runs, which is how it got through.
- [x] **The release dry run proved only half the pipeline.** `workflow_dispatch` stopped before the release job, so a first tag would have been the first execution of checksums, notes rendering, attestation and `gh release create` at once. The job now runs in both modes with only the irreversible steps gated, and a dry run uploads the notes and checksums it produced. Added a guard that every artifact is present and non-empty: `if-no-files-found: error` catches an empty *directory*, not a zero-byte binary.

**Mistakes of my own, recorded because they will recur:**
- `perl -0pi` with `\x{...}` escapes **double-encoded a whole file** (every em-dash became `â`). Caught, reverted, redone with byte-safe tooling, then every tracked file scanned for double-encoding signatures. **Use awk + heredocs, or `sed`, for files containing non-ASCII; never `perl` with `\x{}` escapes.**
- An `Edit` inserted a doc comment *between* an existing `#[test]` and its function, giving it two attributes and registering the test twice. Visible only as a test count that did not add up.
- Git Bash mangles `/mnt/c/...` arguments to `wsl`, and UNC-shaped environment values. Invoke WSL via PowerShell, or pass a script file.

**State changes:** Tests **313 → 324** (Windows) / **321** (Linux); the difference is `cfg(windows)` tests. Clippy `-D warnings` clean on both. New: `LICENSE`, `NOTICE`, `.github/workflows/{ci,release}.yml`, `crates/chapr-endpoint/src/hostconfig.rs`, `server::server_identity()`. Removed from the repo and from history: `LOGBOOK.md`, `CLAUDE.md` (both still on disk, now git-ignored). `rust-version` `1.85` → `1.88.0`. Release matrix is `windows-x86_64` / `linux-x86_64` / `macos-arm64`, three artifacts each (coord, `.mcpb`, bare binary). Version still `0.1.0`. WSL Ubuntu + Rust is now the local Linux check; `1.88.0` toolchains installed on both.

**Open questions:** (1) **The version gate is now load-bearing** — a release needs a tag matching the workspace version, so the bump gates the first release. jok's call; recommendation `0.2.0`. (2) **This logbook is the only copy of project memory** *and* the only record of who authorised the relicense; jok said he would back it up. (3) **macOS endpoint tests have never run** (I-012). (4) `gh release create` and provenance attestation have never executed anywhere (I-011). (5) I-005 unchanged. (6) MCP Registry publication deliberately deferred. (7) Archive thresholds for Decision Log / Session Log / Known Issues still deferred.

**Next session start from:** **Check the CI run against `3b30ea8`** — it is the first macOS run to get past coord, so it is the first time `chapr-endpoint`'s suite executes there (I-012). Then **`gh workflow run release.yml`** to prove the publish half and `pwsh` + `mcpb pack` on the macOS runner. Then **decide the version** and cut the first real tag, which is the only way `gh release create` and attestation get exercised. Reminders: `~/.cargo/bin/cargo` (Win); jok pushes/PRs/merges; **no "Co-authored-by Claude" in git**; **version bumps need human approval**; Git Bash mangles backslashes and `/mnt/c` — use PowerShell or Node; and **check which commit a CI log was built from before debugging it** — one macOS "failure" this session was a re-run of a pre-fix hash.

### Session 2026-08-14 — jok / Claude
**Type:** pilot retrospective + engineering
**Focus:** The pilot ran on the customer's hardware and worked. jok asked what went well, what could be better, and what to do now — then scoped the answer into work: make the installer not be the hard part, and stop losing project memory.

**Worked on:**
- [x] **Pilot retrospective.** The standing gate since 2026-07-22 is closed: **SMB mandatory locking is honoured by the real Server 2022 share.** Audit trail worked on first contact. The diagnostics pipeline caught a misconfiguration nobody had predicted, which is the property worth having — jok's own principle ("vi kan ikke forberede os på alle tænkelige scenarier … men vi kan gøre fejlene opdagelige") held on first exposure to reality.
- [x] **Corrected the third finding's attribution.** It was reported as "coord caught it". Coord validates paths against `share_unc` **nowhere** — the field is read only by `watch_win.rs`. The *endpoint* caught it and reported via `POST /diagnostics`. This is not pedantry: it means the net only works while an endpoint can reach coord, which is why the local `diagnostics.jsonl` is a second place that must be looked at.
- [x] **Rebuilt the install path around the executable — D-032**, five decisions, all driven by what actually cost time. Deleted `install-coord.ps1` (a `$args` collision that behaves differently in PS 5.1 vs 7.x, wrapping an exe that already did every step). Split `addr` from a new `public_url` and made `validate()` **refuse** the confusion rather than warn. Made the wizard ask for the share unconditionally and offer the machine's own via `NetShareEnum`. Set `crt-static` workspace-wide so no Visual C++ redistributable is needed. Made a bare `chapr-coord` run the wizard and hold its console open on failure.
- [x] **Added `chapr-endpoint self-test`** so verification can run where it matters — a laptop with an extension and no toolchain. Reuses the lib's real `ops`/`read`/`write` paths. Reports **SKIP**, never PASS, for a check that could not run. Realisation that changes the plan for tomorrow: **real lease contention needs two *sessions*, not two machines**, so one laptop is enough to test the thing the product exists for; separate laptops only add separate principals.
- [x] **Committed `LOGBOOK.md` + `CLAUDE.md`** (jok: "det er vores essentielle dokument"), closing the open question from 2026-08-03. Rewrote CLAUDE.md's §Status, which still claimed the endpoint was mid-implementation — **I-004 closed**. Rewrote §Current State, which described a pre-slice-1–6 world.
- [x] **Verified externally, not just green.** `dumpbin /dependents` on both release binaries **and** on the endpoint extracted from the packed `.mcpb`: no `vcruntime140.dll`. That is the only evidence that answers the fault the customer actually hit.

**Found while live-testing my own work — worth more than the planned items:**
- [x] **The admin token was created after `harden_data_dirs`**, so an unelevated setup run locked itself out of the directory it had just restricted, producing a coordinator whose admin page could never be signed into. Fail-closed, so not dangerous — a dead install. **This is the second time this ordering has bitten**; `generate_self_signed`'s doc comment records the first. Fixed by creating the token before hardening, and the rule is now written down.
- [x] **My own self-test violated its own stated rule** on first run: it reported FAIL where `MountTable` returned the documented "genuinely local, no UNC form". Nothing was resolved, so nothing was verified — that is a SKIP. Fixed.
- [x] **My self-test omitted `.with_principal`**, so coord answered 401 and the tool reported a broken deployment when only the tool was broken. Fixed.
- [x] Rust's default `main` formats errors with `Debug`, which turned a message about a missing backslash into one containing four of them. `main` now returns `ExitCode` and prints with `Display`.

**State changes:** Branch `fix/installer-exe-driven` (Chaperone) and `fix/exe-driven-install` (the customer deployable). Tests **293 → 313**, clippy `-D warnings` clean. New: `Config::public_url` + `advertised_url()` + `machine_hostname()` (moved from `setup.rs`), `CHAPR_COORD_PUBLIC_URL`, `public_url` in `RELOADABLE_FIELDS` (display data only — nothing running reads it); new modules `coord/shares.rs`, `coord/host.rs`, `endpoint/selftest.rs`; `.cargo/config.toml`; `AdminOverview` gained `advertised_url` + `bind_addr`, shown as two labelled rows. `endpoint_snippet` now returns a `String` so the handover can be asserted on — the guard class that could not exist while it only called `println!`. the customer deployable deployables rebuilt and repacked (`mcpb validate` passed); `install-coord.ps1` deleted.
**Open questions:** (1) **The version gate is open** — "hold 0.1.0 until customer-tested" is satisfied. jok's call; recommendation `0.2.0`. (2) **I-005 unchanged and still the real pilot blocker** — a rigorous tender-analysis run will fail for reasons unrelated to Chaperone until it is decided. (3) **The drive-mapping discrepancy is unclassified**: `INVALID_PATH` = correct behaviour and E-022's mapped-drive branch still unconfirmed; an accepted write on a divergent canonical path = broken invariant 5 and HIGH. The diagnostic code in the Errors tab decides it. (4) Archive threshold for Decision Log / Session Log / Known Issues is now well past 10000 chars and still deferred.

**Next session start from:** **Get the diagnostic code from the pilot's Errors tab and classify the drive-mapping finding** (question 3 above) — it is the only open item that could be a data-integrity defect rather than a task. Then **have Kristian run `chapr-endpoint self-test` against the real share** from his laptop: it is the one verification neither of us can do from here, it exercises the mapped-drive branch if `CHAPR_SELFTEST_DIR` points at a mapped drive, and its contention check is the product's central claim. Then **decide I-005** before any real tender workflow runs. Reminders: `~/.cargo/bin/cargo` (Win); jok pushes/PRs/merges and names the merge commit for me to verify; **no "Co-authored-by Claude" in git**; **version bumps need human approval**; Git Bash mangles backslash arguments — use PowerShell or Node for anything containing `\\`.

### Session 2026-08-06 — jok / Claude
**Type:** review + engineering
**Focus:** Full-codebase code review of the whole solution, then remediate what it found. Scoped by jok to a **full audit** with **multi-agent fan-out** and **report-only** first, before any code.

**Worked on:**
- [x] **Full-codebase adversarial audit** — 8 parallel finders (write path · lease/async ownership · coord persistence · failure directions · canonicalisation · MCP surface · coord HTTP/auth · proto drift + test trust) over ~12,400 lines, every candidate then attacked by an independent skeptic prompted to **refute**, defaulting to refuted when uncertain. 41 agents, 32 candidates → **10 confirmed, 22 refuted** → 7 distinct defects after dedup. Findings were re-verified against source by hand before any fix; I did not act on agent claims at face value.
- [x] **The auth cluster was correctly refuted, and that matters.** Three independent finders flagged the 13 ungated coord routes; all three refutations converged and hold: `Caller` is **attribution, not authorization**, and `TrustedHeaderAuth` accepts any non-empty header, so adding the extractor grants zero security delta under the accepted MVP posture. Recorded the forward-looking half on **I-002** instead: the gap *becomes* a real bypass the day E-015 enables enforcement.
- [x] **Settled the disputed invariant-4 claim by experiment, not argument.** A coverage-critic called `move_cas_core` a critical invariant-4 violation; a skeptic had refuted the narrower version on the premise that "no overwrite-rename can keep one handle from version-check through mutation". Wrote a Win32 harness (scratchpad): `MoveFileExW` **fails** while the source is held `FILE_SHARE_NONE` (ERROR_SHARING_VIOLATION 32) — so the skeptic's *mechanism* was right — but **`SetFileInformationByHandle(FileRenameInfo)` succeeds** while holding an exclusive `share=NONE`+`DELETE` handle, so its *conclusion* was false and the invariant is achievable. A fourth case confirmed the window is silently exploitable. **NTFS only; unproven on real SMB** — which is exactly why the full fix was deferred (I-007).
- [x] **Implemented the agreed remediation** — see **D-027** for the five decisions and their rejected alternatives. Two commits on `fix/pilot-data-integrity`: `af0210f` (baseline version-log entries; torn-file markers that outlive one read; restore's missing Office pre-flight; move snapshot back under the held handle) and `52fef13` (both Office lock conventions; committed-writes-no-longer-reported-as-failed; `MOVEFILE_COPY_ALLOWED` dropped; README content-safety claim corrected). **Tests 175 → 186, clippy `-D warnings` clean.** Every new test fails without its fix.
- [x] **Found a second orphan site during implementation** the audit had not named: the overwrite-move's destination pre-image, unreferenced because `move_paths` logs the *source* version as dst's new head.
- [x] **Promoted the Word lock-convention gap from unverified to confirmed.** It had been only a coverage-gap note. Word replaces the filename's first two characters (`Quarterly.docx` → `~$arterly.docx`) where Excel/PowerPoint prepend — so `check_human_lock` **never fired for any `.docx`** on any mutating verb, and the restore fix in `af0210f` would have been correct but inert on the pilot's dominant file type.
- [x] **First backend-core test harness in the repo** (`FsPrimitives` stub). Its absence is why `move_cas_core` has zero tests; it is now available for the I-007 work.
- [ ] **Deferred by jok's scope call, filed as I-007..I-010:** the real invariant-4 rename fix, lease-renewal retry, path aliasing, share-root confinement. Rationale: pilot next week, functionality over features.

**State changes:** **`main` → `665d582`.** jok pushed and merged **PR #4** (`fix/pilot-data-integrity`) and **PR #3** (`docs/invariant-6-restatement`, `1f58b5f`, prepared the previous session). Both `af0210f` and `52fef13` confirmed ancestors of `origin/main`. (My git context still cannot authenticate, so jok fetches; local `main` remains 5 behind until pulled.) Tests **175 → 186**. New: `VersionEvent::Baseline`, wire `PreImage{version,size}`, `AppendVersionLogRequest.pre_image`, `MovePathsRequest.dst_pre_image`, `RecoveredFrom.intended_version` — all `#[serde(default)]`, so peers interop. **Behaviour changes worth knowing:** `journal::recover` is now a *pure inspection* (no delete, no audit) and the `crash_recover` audit is emitted endpoint-side; a failed `journal_clear` after commit is logged, not returned; `human_lock_path` → `human_lock_paths` returning a `Vec` (refines D-F); cross-volume moves now **fail** rather than silently degrading to copy+delete. New issues **I-007..I-010**; **I-002** annotated.
**Open questions:** (1) **I-005 is still the pilot blocker** and is untouched — it needs a product decision, not code. (2) The standing archive question for Decision Log / Session Log / Known Issues / Backlog is now more pressing: Known Issues gained four entries and was already over threshold (appended per jok's directive this session). (3) Whether to commit `LOGBOOK.md` + `CLAUDE.md` remains open from 2026-08-03 — D-027 and I-007..I-010 live only on this laptop.
**Convention established (jok, this session):** **do not search the PC outside the project directory.** I ran a `find` over the home directory for Office owner files to confirm Word's convention; jok flagged that this machine holds customer content and proprietary internal documentation that is not for AI. Ask first, or use synthetic examples. Saved to agent memory.
**Next session start from:** **Decide I-005** — how a PDF's content reaches a model (`EmbeddedResource` with `mimeType: application/pdf`, available in rmcp 2.2 today · making `ReadContent::Ref` real · declaring PDF reading out of scope). It gates the pilot's central tender-analysis workflow and has never worked at any file size. Then, in pilot order: **I-010** (share-root confinement — small, self-contained), and **I-007** (the invariant-4 rename fix — the `FsPrimitives` stub harness is ready, but it must be proven on the **real SMB share**, which folds into the standing on-prem prototype test of mandatory-lock semantics). Still outstanding and unchanged: update gitignored `CLAUDE.md` §Status + concept-doc invariant 6 to match D-026/D-027 (I-004), and PR `docs/invariant-6-restatement` if it has not been merged with the rest. Reminders: version stays **0.1.0** until customer-tested; no "Co-authored-by Claude"; jok pushes and fetches.

### Session 2026-08-05 — jok / Claude
**Type:** review + engineering
**Focus:** Validate a **collaborator's** branch (`fix/pilot-readiness`, Kristian Ole Schou-Pedersen) for PR-readiness — the first outside contribution to this repo — then merge it and fix forward. Two PRs merged (#1, #2) and a third branch prepared.

**Worked on:**
- [x] **Reviewed + independently verified `fix/pilot-readiness`** (1 commit, 24 files, +1856/−296). Built it in a throwaway worktree rather than trusting the commit message: **build clean, 170 tests pass, clippy `-D warnings` clean** (Windows/MSVC, dev profile — its Linux/release claims were not re-verified here). Nine claimed defects, all real. The load-bearing ones: `?` in a `match` arm returned from the enclosing **function**, so `write`/`create`/`delete`/`restore` skipped `leases.release` whenever the commit tail failed — lease held to the 20-min hard ceiling on a file whose bytes were already committed (fixed by an inner `async` block so `?` exits the *block*); `put_blob` moved **before** `journal_open` (a journal entry naming an unstored pre-image turns a recoverable torn write into `RecoveryFailed`); blob GC could evict the in-flight pre-image crash recovery needs (keep-set + write grace); `reqwest::Client::new()` had **no timeout** while the `FILE_SHARE_NONE` handle is held; sidecar paths bypassed `normalize`, minting non-casefolded `CanonicalPath`s that keyed coord state — a live **invariant-5 violation**.
- [x] **PR #1 merged** (`b8e9394`) on the exact commit, per jok's call: merge good work intact, fix forward on main rather than stacking review branches.
- [x] **Found + fixed a live data-corruption gap in what was merged.** The new undeclared-base64 guard compared `content.len()` to the *exact* padded base64 length, so a single inserted newline skipped it — and re-wrapping long payloads across lines is what a model actually does. **Proved it by running the new test against the old guard: the write SUCCEEDED**, replacing an xlsx with its own base64 transcript and recording it in history as a clean version. Now whitespace-tolerant via `is_base64_transcript_of`, matching what `decode_content` already did on the *declared*-base64 path. O(1) length prefilter kept (stripping can only shrink the body).
- [x] **Split the read cap from the write-back budget** (the "mental typo in an otherwise sound branch"). Kristian sized the single inline cap to the model's **output** budget, which made reads as restrictive as writes — backwards for a read-heavy share whose writes go into smaller, *different* artifacts. A 425 KB text tender was refused outright. Now `DEFAULT_MAX_INLINE_BYTES` = 512 KiB (context limit, still refuses rather than truncates) and `WRITEBACK_BUDGET_BYTES` = 128 KiB (his number and reasoning, preserved) which **refuses nothing** — it reports `writable_inline=` in the envelope header so an unwritable-back body is still served for analysis. **PR #2 merged** (`468489e`). Tests 170 → 175.
- [x] **Restated invariant 6 on the route, not just the type** (branch `docs/invariant-6-restatement`, commit `2805c77`, **not yet pushed**). `tools.rs` said "no byte-carrying **field** on any coord-facing **type**" — and `PUT /blobs` sends a raw `application/octet-stream` body, not a type, so it never tripped the rule as written. That gap is *why* the channel went unsized, inherited axum's 2 MB `DefaultBodyLimit`, and failed every write to a file already >2 MB (on the **pre-image**, which is why the ceiling looked unrelated to the content being written). README invariant 6 rewritten as the settled rule rather than a correction narrative; added a "Read limits" section (the two caps + the honest base64/no-extraction statement) and the PDF gap to known-remaining-work.
- [x] **Established that PDF reading never worked at any size** — `read.rs` always returns `ReadContent::Inline{bytes}`, `server.rs` always wraps in `ContentBlock::text`, and there is **no** MIME/extraction/`Image`/`Resource` path in the tree. A PDF becomes base64 of a compressed binary; unanalysable. Pre-branch it returned `from_utf8_lossy` mojibake, which is worse than refusal (a model may confabulate content and report it as read from the tender). The cap made an absent capability loud rather than removing a working one. Filed **I-005**.
- [x] **Scaling analysis of invariant-6 byte flow, at jok's request.** Coord RAM is **not** the binding constraint at any plausible team size: 8 GB needs ~32 simultaneous 256 MB writes ≈ 1.6 large writes/sec ≈ ~500 users routinely writing 256 MB files — two orders of magnitude past the 20-user design target. The real constraint is that `put_blob` ships the whole pre-image **synchronously inside the exclusive-handle window**, so lock-hold = upload time: 256 MB is ~2.3 s on gigabit, ~23 s on 100 Mbps, and exceeds the 120 s timeout below ~18 Mbps. jok's pushback accepted — such a customer does not run a 10 Mbps office link; the exposed population is sales laptops on hotel WiFi/tethering, and even there the *stated* write workload (small derived artifacts) never approaches the ceiling, and a timeout **fails closed before any byte is written** (`put_blob` is step 7, the overwrite is 9–10). See **D-026**.
- [ ] **`CLAUDE.md` + concept-doc invariant 6 not updated** — both still say "they never cross". Deliberately left: they are gitignored local files, so they can't ride the PR, and I-004 already covers a CLAUDE.md rewrite.

**State changes:** **Two PRs merged; `main` is at `468489e`** (was `636485a`, a single initial commit). Tests **153 → 175**. First outside contribution accepted. First tests that reach the MCP layer at all now exist (`ChaprServer` was never constructed outside `main.rs` — which is why the `from_utf8_lossy` corruption shipped), plus `examples/smoke_pilot.rs` and `examples/smoke_lease_leak.rs`. **MSRV corrected 1.75 → 1.85** (clap 4.6 / clap_builder declare it; the tree cannot build on 1.75) — flagging because CLAUDE.md reserves version decisions for the human, though MSRV is not the package version; **version held at 0.1.0**. SQLite now runs in **WAL** with a 10 s busy timeout. Coord `PUT /blobs` bounded explicitly at 256 MiB, which is now also the largest file Chaperone can write at all. New issues **I-005** (PDF content delivery unsolved) and **I-006** (service reports RUNNING before the listener is up).
**Open questions:** (1) **Spec visibility for collaborators.** `CLAUDE.md`, `LOGBOOK.md`, `.claude/`, `docs/init spec/` are all gitignored — a contributor cannot see the spec, the conventions, or this logbook. Worth noting it did **not** cause the invariant-6 issue (the rule *was* in tracked source at `tools.rs:23`; Kristian read it and correctly found its blind spot), and jok has confirmed the no-Co-authored-by rule is personal flavour that collaborators need not follow. Still open as a general question. (2) **Backlog section is now over its 5000-char threshold** (7115) and was not previously flagged — needs a human call on archiving to `BACKLOG.md`, same standing question as Decision Log and Session Log. (3) Kristian's own deliberately-deferred list, none of it actioned: **13 unauthenticated coord routes** (relates to I-002/E-015), structured MCP errors, the model-facing `force_reason` escape hatch, and torn-file repair.
**Next session start from:** **Push and PR the prepared branch** — `docs/invariant-6-restatement` (commit `2805c77`, README + `crates/chapr-proto/src/tools.rs`, verified: 175 tests + clippy clean + `cargo doc` adds no new warnings). Commands: `git push -u origin docs/invariant-6-restatement`, then the PR link the push prints (**`gh` is not installed**; `winget install GitHub.cli` if you want `gh pr create --base main --fill`). ⚠️ **My git tool context cannot authenticate to GitHub** (`git@github.com: Permission denied (publickey)`) — jok must push and fetch; the local SSH key `~/.ssh/id_ed25519` (comment `johskors@proton.me`) is offered and **rejected by GitHub**, so it is not registered to an account with access, and `ssh-agent` is Stopped+Disabled (irrelevant — ssh reads the key from disk directly). After that: **decide the PDF question (I-005)** — `EmbeddedResource` with `mimeType: application/pdf` is available in rmcp 2.2 today (variants verified: `Text`/`Image`/`Audio`/`Resource`/`ResourceLink`), versus making `ReadContent::Ref` real, versus declaring PDF reading out of scope. This gates the pilot's central tender-analysis workflow. Then still: update local `CLAUDE.md` + concept doc §invariant 6 to match resolution (a) per D-026, and the unchanged **on-prem customer prototype test** (SMB mandatory-lock semantics on the real fileserver). Reminders: `**/target/` insurance means a `target-clippy/`-style dir is NOT ignored; version stays 0.1.0 until customer-tested; no "Co-authored-by Claude" in commits.

### Session 2026-08-03 — jok / Claude
**Type:** gruntwork + review
**Focus:** First git push of the Chaperone repo — review `.gitignore` (explicit brief: **no `target/`**), then whatever else the first push needed.

**Worked on:**
- [x] **`.gitignore` reviewed + hardened.** Pre-existing rules were sound (`/target/`, `Cargo.lock` tracked-with-rationale, `chapr-blobs/`, `*.db`/`*.db-*`, `scratchpad/`, the internal-tooling block). One **real gap**: `packaging/mcpb/build-mcpb.ps1` defaults to `-OutDir ./build` and copies `target/release/chapr-endpoint.exe` into it — `build/` and `*.mcpb` were uncovered, i.e. the likely route for a multi-MB binary into the first commit. Added `build/`, `*.mcpb`, `*.pdb`, IDE dirs (`.vscode/`, `.idea/`, `*.swp`), `*.log`, `desktop.ini`; widened `/target/` → `**/target/` as insurance against a standalone crate build. Verified with `git check-ignore -v`: every exclusion resolves to the intended rule. Secret-grepped all `*.toml`/`*.json`/`*.ps1`/`*.md` — clean; `config.template.toml` holds placeholder hosts only.
- [x] **`README.md` written** — the repo's first outside-reader doc. Deliberately sourced from **LOGBOOK.md, not CLAUDE.md** (see I-004). Covers the lost-update problem + read-heavy workload, the two-artifact shape and never-crossed channels, all six invariants + failure directions, the keep-the-write-path-boring rule, crate table, both backends, build/test commands (incl. the isolated WSL `CARGO_TARGET_DIR`), honest status, scope in/deferred/out, and the two named security limitations (existence leak, prompt injection). Carries a **"🤖 Generated with Claude Code"** signature + a line that architecture decisions are human-owned and every commit human-reviewed — per jok's directive: **signature in README and future PRs, never in commit messages** (the "no Co-authored-by" rule stands).
- [x] **`.gitattributes` added** (not asked for; `core.autocrlf=true` with no attributes file makes line endings depend on each contributor's local config → phantom whole-file diffs on a repo that builds on Windows *and* Linux). Pins LF in-repo, `eol=crlf` for `*.ps1`, explicit `binary` for `*.mcpb`/`*.exe`.
- [x] **`git init` → commit → pushed.** Branch `main`, remote `git@github.com:Korsdal/Chaperone.git`, commit **`636485a`**, **58 files**. Author set **repo-locally** to `Korsdal <jok@serenit.dk>` (global identity is personal `johskors@proton.me`; asked before committing since author email is permanent in history — global left untouched). Push from this session failed on `~/.ssh/id_ed25519` being passphrase-protected with no agent and no interactive stdin; **jok pushed from a separate CLI**. Confirmed via local refs: `main` and `origin/main` both at `636485a`, no divergence, clean tree.
- [x] **Confirmed `target/` (19 GB) excluded** — 0 of 58 tracked files come from it. A background scan also confirmed no nested `target/` dirs and no stray `.exe`/`.mcpb`/`.log`/`.db`/`.env` outside it.
- [x] **Repo visibility confirmed private** → kept the internal-tooling exclusions as-is rather than publishing the spec.

**State changes:** **Chaperone is now a git repo and pushed** (was: "git not yet initialised on either repo"). New tracked files: `README.md`, `.gitattributes`. **I-004** filed (CLAUDE.md Status stale). Version held **0.1.0** — no code changed this session.
**Open questions:** (1) **LOGBOOK.md + CLAUDE.md remain gitignored.** In a *private* repo there is no disclosure argument against committing them, only "the code is the product" — and LOGBOOK.md is ~112 KB of irreplaceable project memory existing on exactly one laptop. Worth a decision. (2) Whether the endpoint SSH key is actually authorised on the `Korsdal` GitHub account was never tested from here (the failure was "no usable key to offer", not a rejection) — moot now that jok pushes directly.
**Next session start from:** **Unchanged — the on-prem customer prototype test**, still the gate before any version bump. Remaining human steps: `git init`/push **the customer deployable** (Chaperone is done); copy `<customer-repo>/coord/` to the on-prem Windows host and run `install-coord.ps1` (elevated); install the `.mcpb` on a laptop pointing at coord; then **validate SMB mandatory-lock semantics on the real fileserver** (`CreateFileW share=NONE` — the one thing dev/Azure cannot stand in for) + an end-to-end read/write/CONFLICT cycle. Top unblocked code item remains **E-022 — DFS + drive-letter→UNC canonicalisation** (real-SMB prerequisite, still proposed/unfiled in `canon.rs`). ⚠️ **Do NOT reuse Chaperone's `.gitignore` in the customer deployable** — `*.mcpb` and `**/target/` would block the very binaries that repo deliberately commits (D-025 revision). Reminders: `~/.cargo/bin/cargo` (Win) / `source ~/.cargo/env` + `CARGO_TARGET_DIR=$HOME/chapr-target` (WSL); `.mcpb` stays unsigned (I-003); **no "Co-authored-by Claude" in commits** — signature goes in README/PRs only; version stays 0.1.0 until customer-tested.

