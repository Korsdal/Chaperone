# Backlog

> Child document of `LOGBOOK.md` (logbook protocol v1.0).
>
> `LOGBOOK.md` keeps the **live** table only. Everything delivered moves to the
> Delivered appendix below, because completed epics are delivery history — already
> recorded in the Session Log and Decision Log — not backlog.
>
> Split out 2026-08-21, resolving the threshold breach flagged in-file on
> 2026-08-05 (the section had reached 18,357 chars against a 5,000 threshold).

---

## Live

| ID | Task | Priority | Est. Sessions | Status | Notes |
|----|------|----------|---------------|--------|-------|
| E-029 | **Re-evaluate CI and release building** after the 2026-09-10 run | HIGH | 1 | TODO | jok, 2026-09-10, at the end of a day that produced five commits, a new deployable and a new build toolchain. None of it has been near CI. Concretely: (1) the workflows have not run against a single change made today — CI run #16 predates all of it, so "green" describes a tree that no longer exists; (2) the **MSI is a second release artifact nobody builds** — it needs `wix` plus two extensions on the Windows runner, and today it was built by hand on one laptop, which is exactly what D-034 says a release must not be; (3) the **endpoint MCPB is in the same position** — rebuilt by hand today, and the bundle shipped to the rig on 09-10 was 0.1.3 from the day before, which is how a session came to test yesterday's code; (4) the **rig and `verify-msi.ps1` are the real test surface now** and neither is expressible in a hosted runner — the second needs elevation and a clean machine — so what CI is *for* wants restating rather than assuming; (5) three toolchain prerequisites are undeclared anywhere a runner reads: `wix` 6.0.2, its Firewall and UI extensions, and `@anthropic-ai/mcpb`. **Before the next tag, not after:** a release built by a person is a release that cannot be rebuilt. |
| E-030 | **Code signing** for the MSI and the release binaries | LOW | 1–2 | DEFERRED | jok, 2026-09-10: *"not wanted — yet — deep backlog for if this product actually creates demand."* An unsigned MSI shows "Unknown publisher" in the UAC prompt and may raise SmartScreen on the customer's fileserver; an administrator can click past it, and will ask about it. This is a **purchase before it is engineering** — an OV or EV certificate, then a secret and a signing step in the release workflow. Note the `.mcpb` half cannot be fixed by buying anything: bundle signing is broken upstream in the mcpb CLI (**I-003**), so Claude Desktop reports every bundle as unsigned regardless. Until then the honest substitute is `SHA256SUMS` plus provenance attestation, both of which a release already carries, and the release notes say plainly that nothing is signed rather than letting an administrator meet the prompt unwarned. **Trigger to revisit:** a customer asking, or a deployment where clicking past SmartScreen is not acceptable policy. |
| E-015 | Enforced control-plane auth (pluggable): `negotiate` (on-prem) / **generic OIDC** validator + `TokenSource` (cloud/hybrid) | MED | 2–3 | DEFERRED | **D-024** — deferred to production-hardening / security-requiring customers; MVP uses E-023. Banked design (D-023): `jsonwebtoken` validator by config (issuer/JWKS/audience/principal-claim, any IdP); `oid`+display audit identity; `negotiate` reuses the share's Kerberos. Closes I-002 when built. |
| E-028 | **File-extraction MCP for Chaperone** — a separate deployable that turns documents and legacy-encoded text into UTF-8 mirrors Chaperone can serve | MED | large | TODO | **D-039**, jok 2026-08-25: *"it is not a file extraction service, it is a coordination service."* Chaperone refuses what it cannot hand over as text — PDFs and Office documents (I-005) and text in a non-UTF-8 encoding (I-015) — and that refusal is the boundary working, not a gap. This is where the capability goes when it is wanted. **Deliberately a separate artifact:** it iterates without a fleet rollout, avoids endpoint/extractor version skew, keeps the coordinator lean, and lets a customer bring their own pipeline instead — the same reasoning the roadmap review reached for extraction-as-a-third-deployable. Scope when it starts: document → text mirror (what the customer's own pipeline does today, per D-028), and **transcoding legacy-encoded text to UTF-8**, which is the smaller and more mechanical half — `std`'s `String::from_utf16` covers UTF-16, and CP1252 is a fixed 27-entry table over `0x80..=0x9F` plus the identity map above it, so neither needs a dependency. **What it must not do:** guess an undeclared encoding statistically (`chardetng`), which trades a loud refusal for quiet mojibake a model reads straight past. **Prerequisite thinking, not code:** anything that *writes* a transcoded file has to answer what the write side does — `decode_content` can only emit UTF-8 today, so a transcode-on-read with no matching write is how a file silently changes encoding under the user's own principal (see I-015's write-side note). Mirror *coordination* is a separate open question (D-028, roadmap 3.4/4.x) and this does not settle it. |
| E-020 | SQLite backend-registry table + admin API (runtime-mutable routing) | LOW | 1–2 | TODO | Deferred behind `index::backend_for` (D-B) — a drop-in swap, invisible to proto/endpoint. Build only when a customer needs runtime-managed/per-resource routing (dashboard). |
| E-021 | Relax `VersionToken` to backend-opaque (BLAKE3 for synth, ETag for cloud) | LOW | 1 | TODO | Deferred until a cloud backend lands (D-019 fork). Drop the 64-hex validation; keep `hash()` for synth. `token_kind` descriptor already in place. |
| V3-cloud | Cloud backends S3 → Azure → Graph (conditional-PUT / lease / ETag adapters + cloud-event watchers) | LOW | large | TODO | The V3 expansion. Depends on E-018 (done) + E-017 + E-021. See D-019 / plan file. |
| E-024b | Coord dashboard / admin UI | LOW | large | DEFERRED | D-030: explicitly **not pilot work**. Depends on E-024a. Deferred precisely because a UI is where a pilot plan slips. |
| E-022 | Drive-letter → UNC canonicalisation (+ DFS, now droppable) | ~~HIGH~~ — | 1 | **DONE, fully verified 2026-09-08** | Deferred in `canon.rs`; verified 2026-08-12 that `WinGrammar::normalize` (`pathgrammar.rs:117`) silently accepts and lowercases a drive letter rather than rejecting it, so two laptops with different mapped letters key one file twice. Live bytes stay safe (invariant 3), but torn-file markers and conflict entries become invisible under the other alias, and the coord watcher — which keys UNC — invalidates nothing. **DFS half droppable:** the pilot customer confirmed no DFS-N and no DFS-R. **Built 2026-08-12, `d887eb6`:** new `mount` module (`MountTable` trait + `WinMountTable` over `WNetGetUniversalNameW` + `NoMountTable`), impure step split out as `PathGrammar::to_rooted` so `normalize` stays pure and cross-platform testable, short-circuit on already-UNC paths (≤1 syscall per user-supplied path). Resolve-when-possible / refuse-loudly-when-not: a relative or drive-relative path is rejected with an explanation, a lookup failure is an error rather than a silent fall back to the aliasing spelling, and a genuinely local path is left alone on purpose (cannot be shared between laptops, and the live-smoke harness runs on local temp trees). **⚠ Residual: the mapped-drive branch is verified against a fake table only.** The dev machine has no network mappings and no rights to create one; the real FFI is covered by a test asserting `C:\Windows` classifies as local (buffer sizing, `u64` alignment, `ERROR_NOT_CONNECTED` mapping). **Add to the on-prem visit: map the pilot share and confirm one resolution against the real server.** **RESIDUAL CLOSED 2026-09-08 (D-045):** the local Hyper-V rig supplied the mapped drive nobody had. `Z:\` resolved to `\\CHAPR-FS\chaprtest\` through the real `WNetGetUniversalNameW` against a real Windows Server 2022 — the first time this branch has executed outside a fake table. Two things cover it permanently now: an opt-in `#[ignore]`d test (`mount::tests::real_mapped_drive_resolves_to_unc`, driven by `CHAPR_TEST_MAPPED_DRIVE`, asserting shape rather than a literal so it works against any rig), and the **whole write path** exercised through the letter — the shipped self-test pointed at the mapped drive reported 9 passed / 0 unverified / exit 0, versus 8 passed / 1 unverified / exit 1 for the same infrastructure addressed by UNC. CI now maps a drive too (D-045(3)), so this branch runs on every push instead of being skipped. |

---

## Delivered

Kept verbatim. Ordered as they stood in `LOGBOOK.md`.

| ID | Task | Priority | Est. Sessions | Status | Notes |
|----|------|----------|---------------|--------|-------|
| E-001 | Scaffold Cargo workspace + `chapr-proto` (record types, exhaustive error enum, serde round-trip) | HIGH | 1–2 | DONE | 2026-07-21. 7 modules, 21 tests green, clippy clean. See D-002. |
| E-002 | `chapr-coord` coarse-locked lease table over SQLite + two axum endpoints (acquire/release) | HIGH | 2–3 | DONE | 2026-07-21. 11 tests, clippy clean, binary smoke-tested. See D-003, I-001. |
| E-003 | Add lease renewal + version index (BLAKE3 hash cache) to coord | MED | 1–2 | DONE | 2026-07-21. renew + reaper + version index/`resolve`/refresh. 24 coord tests, clippy clean, smoke-tested. See D-004. |
| E-004 | Intent journal + lazy/proactive crash recovery in coord | MED | 2 | DONE | 2026-07-21. Journal + 3-state read machine + startup scan (detection only). 31 coord tests, smoke-tested across restart. See D-005. |
| E-006 | Content-addressed history / blob store + version log (coord) | MED | 2–3 | DONE | 2026-07-21. File-per-blob + version_log + store/fetch/history endpoints. 37 coord tests, smoke-tested (dedup, on-disk, chain, 404). See D-006. |
| E-005 | `chapr-endpoint`: canonicaliser, coord client, read path (§8), windows-rs write path (§7), lease-renewal thread (§9) | HIGH | 3+ | DONE | 2026-07-21, slices 1–4. Serves chapr.read/write/conflicts/resolve_conflict over MCP; background lease renewal. Verified live. See D-007/D-008/D-010/D-011. |
| E-011 | Tool verbs `chapr.list`/`stat`/`history`/`create`/`delete`/`restore` | MED | 2–3 | DONE | 2026-07-21. `ops` module + read-family; no proto/coord changes. 31 endpoint tests, full live lifecycle (create→write→history→stat→delete→read-gone→restore→list). See D-012. |
| E-007 | Blob GC + retention (§12) | LOW | 1–2 | DONE | 2026-07-21. `gc` module: refcount mark-and-sweep (floor + age + orphan + ceiling), bytes-only, background timer. 62 coord tests. Numbers still open item #2. See D-017. |
| I-001/I-002 auth | Pluggable auth boundary (dev trusted-header + Negotiate stub) | MED | 1 | DONE | 2026-07-21. `auth` module + `Caller` extractor on all principal handlers. Live-verified. See D-016. |
| E-023 | **MVP identity: zero-setup ambient OS identity** (endpoint derives logged-in Windows principal → coord `trusted-header`) | HIGH | 0.5 | DONE | 2026-07-22. `identity` module: `USERDOMAIN\USERNAME` (Win) / `$USER` (Unix), `CHAPR_PRINCIPAL` override; no new deps, no Win32 interop. 4 tests; live-verified (auto-derived `AzureAD\JohannesKorsdal`, no config). Coord uses existing `trusted-header`. See D-024. |
| E-016 | Coord installer + service + TLS | MED | 2 | DONE | 2026-07-21. `chapr-coord setup` wizard, TOML config (defaults→file→env), rustls TLS, native Windows SCM + systemd, self-test. coord 72 tests; live-verified (setup→serve→healthz, HTTPS). See D-018. |
| E-017 | Push-based `WatchSource` + `POST /watch/event` (unlocks Opt-4 Linux coord + per-backend/cloud watchers) | MED | 0.5 | DONE | 2026-07-22. `POST /watch/event` (direct `watch::apply`, coord-local DTO, `Caller`-gated); `mod watch` ungated; dead-code guards. +3 wiring tests; live-verified Win+Linux. See D-022 / plan file. |
| E-018 | Backend-adapter refactor: `Backend` trait (endpoint) + coord backend-discovery seam | HIGH | 2–3 | DONE | 2026-07-21. `SmbBackend` sole impl, server `Arc<dyn Backend>`, coord announces on `resolve`, wizard `--backend`. Behaviour-identical; live smoke 14/14. See D-020 (forks D-A/D-B). |
| E-019 | POSIX backend (first second-backend: advisory flock + inotify + synth-CAS) | MED | 2 | DONE | 2026-07-22. Shared §7 core (`LockedFile`/`FsPrimitives`/`PathGrammar`); `PosixBackend`+`posixfs` (fs4 advisory flock)+`PosixGrammar`; `BackendKind::Posix`; `CHAPR_BACKEND` selection; `cfg(windows)`-gated SMB. Dual-platform live 14/14. See D-021 / plan file. |
| E-012 | `chapr.move` — atomic dual-lease rename (§6.3) | MED | 2 | DONE | 2026-07-21. `mv` coord module (atomic re-key tx) + `MoveFileExW` + dual-lease + CAS both sides. 48 coord tests, live plain+overwrite move. See D-013. **v1 tool surface complete.** |
| E-008 | Wire audit events into all coord ops + endpoint | MED | 1–2 | DONE | 2026-07-21. lease_grant/renew/expire (session-on-lease), write_commit (POST /audit). restore unwired (no op yet). See D-010. |
| E-009 | Conflict registry (§11) | MED | 2 | DONE | 2026-07-21. Store + surface-on-touch + chapr.conflicts/resolve_conflict + audit. Watcher-inferred close deferred (needs Win32). See D-010. |
| E-010 | Structural read-before-write (§6.2) | MED | 1 | DONE | 2026-07-21. `session_reads` table + `reads` module (record/assert) + `ReadReceipt`; chapr.read & write/create commits record, write/delete/move assert. coord 51 tests; live-verified (fabricated base_version rejected). See D-014. |
| E-025 | **Coordinated-root config** — enforce it (closes I-010 confinement) **and** announce it in the MCP `instructions` (D-028) | HIGH | 1 | DONE | 2026-08-12, `d887eb6` (slice 2, branch `fix/path-identity`). `CHAPR_ROOT` (comma-separated) → canonical roots fixed once in a `OnceLock`, checked inside `canonicalize` *after* resolution so it composes with E-022. Unset = unconfined + start-up warning; set-but-unusable = **fatal** (an operator who configured a root must not silently get an unconfined endpoint). `server::instructions()` now announces the root and carries D-028's write-routing rule + D-030's fan-out rule, both asserted by tests. MCPB manifest gained a "Coordinated location" field — without it the feature is unreachable in the deliverable. **Closes I-010.** |
| E-024c | **Admin page** — 5 read-only tabs served by coord at `GET /admin` + the 3 read routes behind them | HIGH | 1 | DONE | 2026-08-12, `8dbde25` (slice 5, branch `feat/admin-ui`). **Why it outranked everything else left (jok's call, and the argument that settled it): E-026 was write-only** — the store existed and the only reader was a curl. Overview · Errors · Conflicts · Leases · Audit. One self-contained `include_str!` file, vanilla JS, no CDN/framework/build — a test asserts it references nothing external, since that property is cheap to lose and expensive to discover at a customer. **Data never touches `innerHTML`** (`el()` sets textContent); verified live against a seeded diagnostic whose path/title/detail/facts were HTML injection payloads — rendered as characters, document title untouched. New coord-local DTOs (D-022 precedent: the consumer is JavaScript): `POST /leases/query` (**did not exist at all**; folds an all-or-none set into one row), `POST /audit/query` fleet-wide form (the per-path governance read keeps its exact unbounded behaviour — a truncated "who changed this file" is a wrong answer), `GET /admin/overview` (counts **+ what this coord is configured for**, which is both the first support question and what a Settings tab needs first). `Config.source` added (`#[serde(skip)]`) so the config file's path is knowable. **Deliberately out:** all mutating controls, Endpoints tab (no endpoint registry exists), Settings tab (no config hot-reload), write-throughput chart (no time series — not fabricated). |
| E-024d | **Settings tab + configuration from the browser** | HIGH | 2 | DONE | 2026-08-12, slice 6 (branch `feat/admin-authority`). `GET/PUT /admin/settings` — validate, persist to `Config.source` (temp-then-rename, because this file decides whether the service starts), then apply what can be applied. Only `auth`/`auth_fallback` reload (`RELOADABLE_FIELDS`); everything else is reported as pending a restart rather than implied to be live. **Dual-mode auth cutover** with a two-condition gate — see D-031 for why one condition lies. Env-captive fields are locked and a save that touches one is refused. `AppState` now holds the live `Config` (replacing slice 5's `config_path`/`auth_mode` pair, which could disagree with it). |
| E-024a | **Coord admin role** — enforced gating of the admin routes | HIGH | 1–2 | **DONE (token) / role deferred** | 2026-08-12, slice 6. **Admin token** enforces now: 256 bits in the ACL'd data directory, permanent break-glass, rotatable; `AdminAuth` extractor beside `Caller`; six data routes gated, `GET /admin` deliberately **not** (it is where the token is entered). Fail-closed when no token could be established (503). **`admin_principals` (D-029's role) is carried in config but not yet enforced** — it needs an auth mode that authenticates, i.e. E-015. See D-031. | D-029 + **split per D-030**: the role and the routes ride the pilot round — without them E-026's diagnostics log is unreadable. Authorization axis is a **role declared per deployment**, never OS elevation (D-029 gives the three reasons). **No query routes exist today.** |
| E-026 | **Diagnostics channel** — endpoint reports structured failure events to coord + local file log on the endpoint; separate store from audit | HIGH | 1–2 | DONE | D-030 problem 4. jok's discoverability principle: we cannot anticipate every environment failure, but we can make failures discoverable, which is what makes the unanswered environment questions (FSRM, path length, NFD clients, AV interference) tolerable. **Audit ≠ diagnostics** — separate stores, or audit stops reading as an accountability record. Local file log is load-bearing: a failure before coord connectivity (bad URL, TLS mismatch, blocked port) cannot phone home, and the endpoint's stderr currently goes nowhere as a stdio child of Claude Desktop. **Built 2026-08-12, `0f23844`** (slice 4, branch `feat/diagnostics`). Record shape taken from **jok's admin UI design** (`docs/Chaperone admin design inspiration.html`) so the dashboard needs no migration: **grouped** by `(code, path)` with an occurrence count (one looping agent would otherwise bury everything under identical rows), a required **`remedy`** field (the design's "What fixes it" panel), free key/value **`facts`** (a new environment surprise must not need a schema change), and a `state` column for the deferred acknowledge/resolve transitions. Occurrences carry principal + host — how one misconfigured laptop is told from a fleet-wide fault — capped per group while the count keeps counting. `diag::classify` draws the expected/unexpected line and is hooked into the single funnel every tool failure already passes (complete coverage by construction); an unreachable coord is recorded **locally only**, since it cannot be told it is unreachable. **Deferred: diagnostic *groups* are not swept** — occurrences are capped, but groups grow with distinct `(code, path)` pairs, so a systemic fault across many files accumulates rows. The GC timer is the natural home; retention wants a number validated against real volume first. |
| E-027 | **Intra-session serialisation** — mutex per canonical path in the endpoint + typed "queued" write result | HIGH | 1 | DONE | D-030 problems 1–2. SessionId is per **process** (`main.rs:38`), so a fan-out of subagents shares one lease identity and leases mutually exclude nothing intra-session; today that yields self-conflict sidecars on the happy path and a session-keyed `session_reads` that lets E-010's assertion pass between subagents. Queue result must be a **success carrying instructions** (queued / expected wait / call again after X / do not retry now / do not abandon), never an error — an error triggers abandonment or a retry storm. Terminal state after the per-file budget: "ask the human", never "failed". **Built 2026-08-12, `f854773`** (slice 3, branch `feat/intra-session-serialisation`). **Correction to D-030 problem 1, found by reading `lease.rs` during implementation:** coord's conflict check keys on `path` alone and does **not** exempt the holder's own session, so the intra-session failure was never a conflict sidecar — it was **`LeaseHeld` naming the caller as the holder**, a session told the file was taken by the very user asking. Better for integrity than assumed (nothing written twice), worse for behaviour (an error is what makes an LLM abandon or hammer). **Also NOT fixed, and unfixable by locking:** two subagents that read the same version still make the second write genuinely stale, so it takes the CAS path — designed outcome (bytes parked in a sidecar + re-read guidance), and the reason the `instructions` fan-out rule exists. New `pathlock` module (sorted + de-duplicated lock sets — `tokio::sync::Mutex` is not reentrant and `chapr_move` takes two paths; opportunistic prune on `strong_count == 1`); guards live in the held-lease entry and drop **after** coord's DELETE so the handover is ordered. Retry reuses the pre-existing `RetryBudgetExhausted` (already produced by the read path for the same purpose); **only `LeaseHeld` is retried** — it proves nothing was granted, whereas a lost transport response could hide a granted lease. Budget is a constant (`DEFAULT_ACQUIRE_BUDGET` 30 s), **not env-wired** — `with_acquire_budget` exists for when it is wanted. |
| — | **Tool-level error rendering** (folded into E-027, `f854773`) | — | — | DONE | All **11** tool error arms moved from `McpError::internal_error` to `Ok(CallToolResult::error(..))`. MCP reserves the former for "the call itself broke"; rmcp's own docs call the latter right for almost every "the tool ran and didn't work" case, and it is the form whose content reliably reaches the model. Per-variant guidance answers "did my change land, and what now". **Load-bearing case: `CommittedButUnrecorded`** — the bytes ARE on the share, and reporting failure makes the caller rewrite and conflict against its own committed content. Three existing tests asserted `expect_err` on the protocol Result, correctly caught the change, and now assert `is_error` on the tool result. |
| E-013 | Change-watcher (§14) — index invalidation, watcher-inferred conflict close, overflow rescan | MED | 2 | DONE | 2026-07-21. Trait-isolated on coord (`watch` core + `watch_win` ReadDirectoryChangesW, both cfg(windows)). coord 56 tests; live-verified (out-of-band edit → index invalidated). See D-015. |

---

## Removed duplicates

Preserved rather than deleted, per the logbook rule against destroying entries.

**2026-08-21 — a second `E-007` row, status TODO, duplicating the DONE row above.**
Same scope (its DONE twin records refcount mark-and-sweep with floor, age, orphan and
ceiling all implemented), so the TODO was stale, not outstanding work. Verbatim:

| ID | Task | Priority | Est. Sessions | Status | Notes |
|----|------|----------|---------------|--------|-------|
| E-007 | Blob GC + retention (coord): reference-counted mark-and-sweep across version logs; 90-day byte floor / last-10 / ~50 GB ceiling | LOW | 1–2 | TODO | Split from E-006 (D-006). Not correctness — runaway-protection. Numbers are open item #2 (validate vs real write volume first). Version log is the reference set (no schema change needed). |
