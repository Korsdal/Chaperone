<!--
Three sections, <=6 lines each. A table wherever the rows share a format.

This budget is the point of the template, not a style preference: a wall of text
gets approved without being read, and an approval that did not really happen is
what produces descriptions that drift out of true. If a section does not fit,
cut it rather than growing it — the detail belongs in the logbook entry, which
is written for a reader who has already decided to go deep.

Delete these comments as you fill it in.
-->

## What was the problem

<!-- What was actually wrong or missing. Not what you did about it. -->

## What was changed

<!-- A table when rows share a shape:

| Site | Change |
|---|---|
| `path.rs:12` | one line |
-->

## What was deferred / backlogged

<!-- Every item gets an ID (I-nnn, E-nnn, or the roadmap question), or say
     plainly that it is unbooked. "Nothing" is a valid answer; silence is not. -->
